# lib/Makefile.am

include $(top_srcdir)/Makefile.am.common

# N.B. arla and ko is not listed here because they are dependent on
# components that are dependent on the other libraries so they must
# be compiled after them.
SUBDIRS = $(DIR_roken) bufdir editline $(DIR_sl) util vers
